<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sessions pour le film</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            padding: 20px;
        }

        .session-box {
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 16px;
            margin-bottom: 20px;
            background-color: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .session-box h5 {
            font-weight: bold;
            color: #495057;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="text-center my-4">Sessions pour le film</h1>
        <div class="text-center mb-4">
            <a href="/" class="btn btn-secondary">Retour à la liste des films</a>
        </div>

        <h2 class="mb-3">Liste des séances :</h2>
        <div>
            <% if (sessions.length > 0) { %>
                <% sessions.forEach(session => { %>
                    <div class="session-box">
                        <h5>Session</h5>
                        <p><strong>Date et Heure :</strong> 
                            <%= session.dateTime ? session.dateTime.toLocaleString() : 'Date non disponible' %>
                        </p>
                        <p><strong>Salle :</strong> <%= session.room %></p>
                        <p><strong>Capacité :</strong> <%= session.capacity %> places</p>
                        <p><strong>Technologie de projection :</strong> <%= session.projectionTechnology %></p>
                        <p><strong>Prix du billet :</strong> <%= session.ticketPrice %> €</p>
                        <p><strong>Places disponibles :</strong> <%= session.availableSeats %></p>
                        <p><strong>Disponibilité en ligne :</strong> <%= session.onlineAvailability ? 'Oui' : 'Non' %></p>
                        <div class="text-end">
                            <a href="/reservation/<%= session._id %>" class="btn btn-primary">Réserver</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="alert alert-warning">
                    <p>Aucune séance disponible pour ce film.</p>
                </div>
            <% } %>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
