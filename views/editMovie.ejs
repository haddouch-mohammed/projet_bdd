<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier un film</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container my-5">
        <h1 class="text-center mb-4">Modifier le film : <%= movie.title %></h1>
        <form action="/movies/edit/<%= movie._id %>" method="POST" enctype="multipart/form-data" class="border p-4 rounded shadow">
            <div class="mb-3">
                <label for="title" class="form-label">Titre :</label>
                <input type="text" name="title" id="title" class="form-control" value="<%= movie.title %>" required>
            </div>

            <div class="mb-3">
                <label for="releaseYear" class="form-label">Année de sortie :</label>
                <input type="number" name="releaseYear" id="releaseYear" class="form-control" value="<%= movie.releaseYear %>">
            </div>

            <div class="mb-3">
                <label for="duration" class="form-label">Durée (minutes) :</label>
                <input type="number" name="duration" id="duration" class="form-control" value="<%= movie.duration %>">
            </div>

            <div class="mb-3">
                <label for="genre" class="form-label">Genre :</label>
                <input type="text" name="genre" id="genre" class="form-control" value="<%= movie.genre %>">
            </div>

            <div class="mb-3">
                <label for="director" class="form-label">Réalisateur :</label>
                <input type="text" name="director" id="director" class="form-control" value="<%= movie.director %>">
            </div>

            <div class="mb-3">
                <label for="actors" class="form-label">Acteurs principaux (séparés par une virgule) :</label>
                <input type="text" name="actors" id="actors" class="form-control" value="<%= movie.actors.join(', ') %>">
            </div>

            <div class="mb-3">
                <label for="synopsis" class="form-label">Synopsis :</label>
                <textarea name="synopsis" id="synopsis" class="form-control" rows="4"><%= movie.synopsis %></textarea>
            </div>

            <div class="mb-3">
                <label for="classification" class="form-label">Classification :</label>
                <input type="text" name="classification" id="classification" class="form-control" value="<%= movie.classification %>">
            </div>

            <div class="mb-3">
                <label for="language" class="form-label">Langue :</label>
                <input type="text" name="language" id="language" class="form-control" value="<%= movie.language %>">
            </div>

            <div class="mb-3">
                <label for="trailer" class="form-label">Bande-annonce (lien) :</label>
                <input type="url" name="trailer" id="trailer" class="form-control" value="<%= movie.trailer %>">
            </div>

            <div class="mb-3">
                <label for="poster" class="form-label">Affiche du film :</label>
                <input type="file" name="poster" id="poster" class="form-control">
                <div class="mt-2">
                    <img src="<%= movie.poster %>" alt="Affiche de <%= movie.title %>" class="img-fluid rounded" style="max-width: 200px;">
                </div>
            </div>

            <div class="mb-3">
                <label for="format" class="form-label">Format :</label>
                <input type="text" name="format" id="format" class="form-control" value="<%= movie.format %>">
            </div>

            <div class="mb-3">
                <label for="productionStudio" class="form-label">Studio :</label>
                <input type="text" name="productionStudio" id="productionStudio" class="form-control" value="<%= movie.productionStudio %>">
            </div>

            <div class="mb-3">
                <label for="releaseDate" class="form-label">Date de sortie :</label>
                <input type="date" name="releaseDate" id="releaseDate" class="form-control" value="<%= movie.releaseDate.toISOString().split('T')[0] %>">
            </div>

            <div class="text-center">
                <button type="submit" class="btn btn-primary">Modifier</button>
                <a href="/" class="btn btn-secondary">Annuler</a>
            </div>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
