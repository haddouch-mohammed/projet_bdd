<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails des Notes</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container my-5">
        <h2 class="text-center mb-4">Détails des Notes</h2>

        <div class="alert alert-info text-center" role="alert">
            <strong>Moyenne des Notes :</strong> <%= movie.averageRating %> / 5
        </div>

        <h3 class="mb-3">Détail des Notes :</h3>
        <ul class="list-group">
            <% movie.ratingDetails.forEach((ratingDetail) => { %>
                <li class="list-group-item">
                    <div class="d-flex justify-content-between align-items-center">
                        <span><strong><%= ratingDetail._id %> Étoiles</strong></span>
                        <span class="badge bg-primary rounded-pill"><%= ratingDetail.count %> avis</span>
                    </div>
                    <!-- Afficher les commentaires s'ils existent -->
                    <% if (ratingDetail.comments && ratingDetail.comments.length > 0) { %>
                        <ul class="mt-3">
                            <% ratingDetail.comments.forEach(comment => { %>
                                <% if (comment) { %> <!-- Vérification si le commentaire n'est pas null -->
                                    <li class="mt-1">
                                        <em>"<%= comment %>"</em>
                                    </li>
                                <% } %>
                            <% }); %>
                        </ul>
                    <% } else { %>
                        <p class="text-muted mt-2">Aucun commentaire pour cette note.</p>
                    <% } %>
                </li>
            <% }); %>
        </ul>
        
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
