<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Films</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .movie-box {
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 16px;
            margin: 16px 0;
            display: flex;
            align-items: flex-start;
        }
        .movie-box img {
            max-width: 150px;
            margin-right: 16px;
            border-radius: 8px;
        }
        .movie-info {
            flex-grow: 1;
        }
        .movie-info h2 {
            margin: 0;
        }
        .actions {
            margin-top: 8px;
        }
    </style>
</head>
<body>
   
    <h2>Tous les films :</h2>
    <ul class="menu-area-main">
        <li class="active"><a href="/movies/new">Ajouter un film</a> |  </li>
        <li><a href="/seances/new">Ajouter une séance</a> | </li>
        <li> <a href="#about">About</a> </li>
        <li>  <a href="/">Voir tous les films</a></li>
        <li> <a href="#contact">Contact</a> </li>
       
    </ul>
    <div>
        <% if (movies.length > 0) { %>
            <% movies.forEach(movie => { %>
                <div class="movie-box">
                    <% if (movie.poster) { %>
                        <img src="<%= movie.poster %>" alt="Affiche de <%= movie.title %>">
                    <% } %>
                    <div class="movie-info">
                        <h2><%= movie.title %></h2>
                        <p><strong>Année :</strong> <%= movie.releaseYear %></p>
                        <p><strong>Durée :</strong> <%= movie.duration %> minutes</p>
                        <p><strong>Genre :</strong> <%= movie.genre %></p>
                        <p><strong>Réalisateur :</strong> <%= movie.director %></p>
                        <p><strong>Synopsis :</strong> <%= movie.synopsis %></p>
                        <div class="actions">
                            <a href="/movies/edit/<%= movie._id %>">Modifier</a>
                            <form action="/movies/delete/<%= movie._id %>" method="POST" style="display:inline;">
                                <button type="submit">Supprimer</button>
                            </form>
                            <a href="/seances/movie/<%= movie._id %>">Consulter les séances</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <p>Aucun film disponible.</p>
        <% } %>
    </div>
</body>
</html>

